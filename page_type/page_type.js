/**
 * Created by paquettepy on 2016-09-12.
 */

function PageType(type, domain, regex) {
    var type = type;
    var domain = domain;
    var regex = regex;
    var selectors = {
        "containers": [],
        "items": []
    }
    
}

PageType.prototype.add_selectors(selectors) {
    
}
